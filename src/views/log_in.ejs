<!DOCTYPE html>
<html lang="vi">
  <head>
    <title>Chào mừng đến với Quản trị BlueBook</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600;700;800&display=swap&subset=vietnamese"
      rel="stylesheet"
    />

    <style>
      * {
        box-sizing: border-box;
      }

      body {
        background: #f6f5f7;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        font-family: "Nunito", sans-serif;
        min-height: 100vh;
        margin: 0;
        padding: 30px 0;
      }

      /* ====================== TITLE ====================== */
      body > h2 {
        font-size: 28px;
        font-weight: 800;
        letter-spacing: 1px;
        margin: 10px 0 35px;
        color: #222;
        text-transform: uppercase;
        position: relative;
      }

      body > h2::after {
        content: "";
        width: 80px;
        height: 3px;
        background: #83a5eb;
        position: absolute;
        bottom: -8px;
        left: 50%;
        transform: translateX(-50%);
        border-radius: 10px;
      }

      /* ====================== FORM ====================== */
      form {
        background-color: #ffffff;
        width: 100%;
        max-width: 470px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 0 50px;
        height: 100%;
        text-align: center;
      }

      form h1 {
        font-weight: 800;
        font-size: 27px;
        margin-bottom: 16px;
        letter-spacing: 0.5px;
        color: #111;
      }

      /* Input group */
      .input-group {
        position: relative;
        width: 100%;
        max-width: 530px;
        margin: 14px auto;
      }

      .input-group input {
        width: 100%;
        padding: 12px 48px 12px 16px;
        border-radius: 12px;
        border: 1px solid #d5d5d5;
        background: #f7f7f7;
        font-size: 15px;
        transition: 0.2s ease;
      }

      .input-group input:focus {
        background: #fff;
        border-color: #83a5eb;
        box-shadow: 0 0 0 3px rgba(131, 165, 235, 0.35);
        outline: none;
      }

      /* Icon ẩn/hiện mật khẩu */
      .toggle-password {
        position: absolute;
        top: 50%;
        right: 16px;
        transform: translateY(-50%);
        cursor: pointer;
        color: #7d7d7d;
        font-size: 18px;
        transition: 0.2s ease;
      }

      .toggle-password:hover {
        color: #83a5eb;
      }

      /* ====================== BUTTON ====================== */
      button {
        border-radius: 30px;
        border: none;
        background-color: #83a5eb;
        color: #fff;
        font-size: 1rem;
        font-weight: 600;
        padding: 12px 40px;
        letter-spacing: 0.5px;
        cursor: pointer;
        transition: 0.25s ease;
        margin-top: 12px;
      }

      button:hover {
        background-color: #6f91d6;
        transform: translateY(-1px);
      }

      button.ghost {
        background-color: transparent;
        border: 2px solid #ffffff;
      }

      button.ghost:hover {
        background-color: #ffffff;
        color: #83a5eb;
      }

      /* ====================== ERROR ====================== */
      .login-error {
        text-align: center;
        margin-top: 5px;
        margin-bottom: 5px;
        color: red;
      }

      .login-error p {
        margin: 0;
      }

      #error-message,
      #password-match-error {
        color: #ff4d4f;
        font-size: 15px;
        font-weight: 600;
        margin-top: 6px;
        margin-left: 4px;
        display: none;
        text-align: left;
        animation: fadeErr 0.3s ease;
      }

      /* ====================== CONTAINER ====================== */
      .container {
        background-color: #fff;
        border-radius: 12px;
        box-shadow: 0 14px 28px rgba(0, 0, 0, 0.1),
          0 10px 10px rgba(0, 0, 0, 0.06);
        position: relative;
        overflow: hidden;
        width: 780px;
        max-width: 95%;
        min-height: 520px;
      }

      .form-container {
        position: absolute;
        top: 0;
        height: 100%;
        transition: all 0.6s ease-in-out;
      }

      .sign-in-container {
        left: 0;
        width: 50%;
        z-index: 2;
      }

      .container.right-panel-active .sign-in-container {
        transform: translateX(100%);
      }

      .sign-up-container {
        left: 0;
        width: 50%;
        opacity: 0;
        z-index: 1;
      }

      .container.right-panel-active .sign-up-container {
        transform: translateX(100%);
        opacity: 1;
        z-index: 5;
        animation: show 0.6s;
      }

      @keyframes show {
        0%,
        49.99% {
          opacity: 0;
          z-index: 1;
        }
        50%,
        100% {
          opacity: 1;
          z-index: 5;
        }
      }

      /* ====================== OVERLAY ====================== */
      .overlay-container {
        position: absolute;
        top: 0;
        left: 50%;
        width: 50%;
        height: 100%;
        overflow: hidden;
        transition: transform 0.6s ease-in-out;
        z-index: 100;
      }

      .container.right-panel-active .overlay-container {
        transform: translateX(-100%);
      }

      /* Overlay gradient đậm hơn để chữ trắng nổi bật */
      .overlay {
        background: linear-gradient(135deg, #6f89d8, #476ad1);
        background-size: cover;
        position: relative;
        color: #fff;
        left: -100%;
        height: 100%;
        width: 200%;
        transform: translateX(0);
        transition: transform 0.6s ease-in-out;
      }

      .container.right-panel-active .overlay {
        transform: translateX(50%);
      }

      .overlay-panel {
        position: absolute;
        display: flex;
        align-items: center;
        flex-direction: column;
        justify-content: center;
        padding: 0 60px;
        text-align: center;
        top: 0;
        height: 100%;
        width: 50%;
      }

      /* Chữ rõ hơn + premium */
      .overlay-panel h1 {
        font-size: 32px;
        font-weight: 800;
        line-height: 1.25;
        margin-bottom: 6px;
        white-space: nowrap;
        color: #f0f4ff;
        text-shadow: 0 2px 6px rgba(0, 0, 0, 0.25);
      }

      .overlay-panel p {
        font-size: 18px;
        font-weight: 500;
        line-height: 1.55;
        color: #dde6ff;
        margin-top: 6px;
        margin-bottom: 26px;
        letter-spacing: 0.2px;
        text-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
        opacity: 0;
        animation: fadeUp 0.6s ease forwards;
      }

      @keyframes fadeUp {
        from {
          opacity: 0;
          transform: translateY(6px);
          filter: blur(2px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
          filter: blur(0);
        }
      }

      .overlay-right {
        right: 0;
      }

      /* ====================== RESPONSIVE ====================== */
      @media (max-width: 600px) {
        .container {
          max-width: 100%;
          min-height: 600px;
        }

        .form-container,
        .overlay-container {
          width: 100%;
          left: 0;
          position: relative;
          height: auto;
        }

        .sign-in-container,
        .sign-up-container {
          width: 100%;
          opacity: 1;
          transform: none !important;
          z-index: 5;
        }

        .overlay-container {
          display: none;
        }
      }

      /* Animation */
      @keyframes fadeErr {
        from {
          opacity: 0;
          transform: translateY(-6px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>

  <body>
    <h2>LOGIN & REGISTER</h2>

    <div class="container" id="container">
      <!-- REGISTER -->
      <div class="form-container sign-up-container">
        <form id="register-form" method="POST" action="/register">
          <h1>Đăng ký</h1>

          <div class="input-group">
            <input
              type="text"
              name="taikhoan"
              placeholder="Tên đăng nhập"
              required
            />
          </div>

          <div class="input-group">
            <input
              type="password"
              id="register-password"
              name="matkhau"
              placeholder="Mật khẩu"
              required
            />
            <i
              class="far fa-eye toggle-password"
              data-target="register-password"
            ></i>
          </div>

          <div class="input-group">
            <input
              type="password"
              id="confirm-password"
              name="nhaplaimatkhau"
              placeholder="Nhập lại mật khẩu"
              required
            />
            <i
              class="far fa-eye toggle-password"
              data-target="confirm-password"
            ></i>
          </div>

          <div class="input-group">
            <input type="text" name="hoten" placeholder="Họ tên" required />
          </div>

          <div class="input-group">
            <input type="email" name="email" placeholder="Email" required />
          </div>
          <div class="login-error">
            <% if(registerError === "USER_EXISTS"){ %>
            <p>Tài khoản hoặc email đã tồn tại</p>
            <% } else if(registerError === "PASSWORD_NOT_MATCH"){ %>
            <p>Mật khẩu nhập lại không trùng khớp</p>
            <% } %>
          </div>

          <button type="submit">Đăng ký</button>
        </form>
      </div>

      <!-- LOGIN -->
      <div class="form-container sign-in-container">
        <form id="login-form" method="POST" action="/login">
          <h1>Đăng nhập</h1>

          <div class="input-group">
            <input
              type="text"
              name="taikhoan"
              placeholder="Tài khoản"
              required
            />
          </div>

          <div class="input-group">
            <input
              type="password"
              id="login-password"
              name="matkhau"
              placeholder="Mật khẩu"
              required
            />
            <i
              class="far fa-eye toggle-password"
              data-target="login-password"
            ></i>
          </div>
          <div class="login-error">
            <% if(loginError === "NOT_FOUND" || loginError ===
            "WRONG_PASSWORD"){ %>
            <p>Tài khoản hoặc mật khẩu không đúng</p>
            <% } else if(loginError === "LOCKED"){ %>
            <p>Tài khoản đã bị khóa</p>
            <% } %>
          </div>

          <button type="submit">Đăng nhập</button>
        </form>
      </div>

      <!-- OVERLAY -->
      <div class="overlay-container">
        <div class="overlay">
          <div class="overlay-panel overlay-left">
            <h1>Chào mừng trở lại!</h1>
            <p>Để kết nối, vui lòng đăng nhập bằng tài khoản của bạn.</p>
            <button class="ghost" id="danhNhap">Đăng nhập</button>
          </div>

          <div class="overlay-panel overlay-right">
            <h1>Xin chào!</h1>
            <p>
              Nhập thông tin cá nhân và bắt đầu hành trình trải nghiệm với chúng
              tôi.
            </p>
            <button class="ghost" id="dangKy">Đăng kí</button>
          </div>
        </div>
      </div>
    </div>

    <!-- SCRIPT -->
    <script>
      const container = document.getElementById("container");

      // Lấy mode từ controller
      const mode = "<%= mode %>";

      if (mode === "register") {
        container.classList.add("right-panel-active");
      } else {
        container.classList.remove("right-panel-active");
      }

      // Nút chuyển panel
      const signUpButton = document.getElementById("dangKy");
      const signInButton = document.getElementById("danhNhap");

      signUpButton.addEventListener("click", () =>
        container.classList.add("right-panel-active")
      );

      signInButton.addEventListener("click", () =>
        container.classList.remove("right-panel-active")
      );

      // Icon ẩn hiện mật khẩu
      const togglePasswords = document.querySelectorAll(".toggle-password");
      togglePasswords.forEach((icon) => {
        icon.addEventListener("click", () => {
          const targetId = icon.getAttribute("data-target");
          const input = document.getElementById(targetId);
          const isHidden = input.type === "password";
          input.type = isHidden ? "text" : "password";
          icon.classList.toggle("fa-eye");
          icon.classList.toggle("fa-eye-slash");
        });
      });
    </script>
  </body>
</html>
